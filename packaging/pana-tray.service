[Unit]
Description=pana tray applet
After=graphical-session.target
PartOf=graphical-session.target

[Service]
Type=simple
ExecStart=/opt/pana/bin/pana-tray --socket /run/pana/pana.sock
Restart=on-failure
RestartSec=3

[Install]
WantedBy=graphical-session.target
